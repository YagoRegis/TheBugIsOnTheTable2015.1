#include "theBugAPI.h"

task main() {


     move(14, POWER_NORMAL, FORWARD);
     turn(87, POWER_NORMAL, LEFT);
     Wait(1);
     move(67, POWER_NORMAL, FORWARD);
     claw_control(70, POWER_HIGH, BACKWARD);
     move(60, POWER_NORMAL, BACKWARD);
}
